# Dependencies
node_modules/

# Build output
dist/

# Compiled Go binary
bot/fleet-bot

# Environment / secrets (decrypted)
.env
*.env
*.secrets

# Vault — instance-specific encrypted secrets
vault/*.age
vault/*.age.bak
vault/manifest.json

# Instance-specific data
data/registry.json
data/registry.json.bak
data/registry.json.tmp
data/registry.json.pre-*
data/deps-cache.json
data/deps-config.json
data/notified-findings.json
# Audit log + rotation snapshots (instance-specific)
vault/.snapshots/

# Private / instance-specific
docs/superpowers/
deploy-webhook.cjs
packages/
npm-downloads/

# Claude Code local settings
.claude/

# Lock files (uncomment if not needed)
# package-lock.json

# OS files
.DS_Store
Thumbs.db
